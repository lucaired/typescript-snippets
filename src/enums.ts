// Possible enums in TypeScript
// Enums allow us to define a set of named constants.
module Enums {
    export enum Color { Red, Green, Blue }
    export enum Direction {
        Up = "UP",
        Down = "DOWN",
        Left = "LEFT",
        Right = "RIGHT"
    }
}